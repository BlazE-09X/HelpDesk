<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@android:color/white"
    android:padding="20dp"
    tools:context=".admin.AdminRegisterUserActivity">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Registration"
        android:textColor="#333333"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/tv_title"
        app:layout_constraintBottom_toTopOf="@id/btn_create">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView style="@style/LabelStyle" android:text="Имя пользователя" />
            <EditText android:id="@+id/et_name" style="@style/EditTextStyle" android:inputType="textPersonName" />

            <TextView style="@style/LabelStyle" android:text="Фамилия пользователя" />
            <EditText android:id="@+id/et_surname" style="@style/EditTextStyle" android:inputType="textPersonName" />

            <TextView style="@style/LabelStyle" android:text="Отчество (если есть)" />
            <EditText android:id="@+id/et_patronymic" style="@style/EditTextStyle" android:inputType="textPersonName" />

            <TextView style="@style/LabelStyle" android:text="Логин" />
            <EditText android:id="@+id/et_login" style="@style/EditTextStyle" android:inputType="text" />

            <TextView style="@style/LabelStyle" android:text="Пароль" />
            <EditText android:id="@+id/et_password" style="@style/EditTextStyle" android:inputType="textPassword" />

            <TextView style="@style/LabelStyle" android:text="Почта/Email" />
            <EditText android:id="@+id/et_email" style="@style/EditTextStyle" android:inputType="textEmailAddress" />

            <TextView style="@style/LabelStyle" android:text="Номер телефона" />
            <EditText android:id="@+id/et_phone" style="@style/EditTextStyle" android:hint="+7(___) ___ __ __" android:inputType="phone" />

            <TextView style="@style/LabelStyle" android:text="Отдел" />
            <Spinner android:id="@+id/spinner_dept" style="@style/EditTextStyle" android:background="@drawable/edit_text_border" />

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btn_create"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:backgroundTint="@color/figma_blue"
        android:text="Создать"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toTopOf="@id/tv_back" />

    <TextView
        android:id="@+id/tv_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:text="Назад"
        android:textColor="#B0B0FF"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>